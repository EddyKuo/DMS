# DMS Frontend - 文件管理系統前端

<p align="center">
  <strong>現代化文件管理系統 | Modern Document Management System</strong>
</p>

---

## 📋 專案簡介

DMS (Document Management System) 是一個功能完整的文件管理系統前端應用程式，採用現代化科技感 UI 設計，提供直覺的檔案瀏覽、上傳、版本控制及分享功能。

### 主要特色

- 🎨 **科技感深色主題** - 支援明暗主題切換
- 📁 **檔案總管介面** - 類似桌面系統的操作體驗
- 🔄 **版本控制** - 完整的檔案版本歷程管理
- 📤 **拖曳上傳** - 支援 Drag & Drop 批次上傳
- 🔗 **分享連結** - 產生可設定過期時間的分享連結
- 🏷️ **標籤管理** - 為檔案新增/刪除標籤
- 🌐 **多語系** - 支援繁體中文、簡體中文、英文
- 📊 **儀表板** - Recharts 圖表顯示儲存空間統計

---

## 🛠️ 技術架構

### 核心框架
| 技術 | 版本 | 用途 |
|------|------|------|
| React | 19.x | UI 框架 |
| TypeScript | 5.9 | 型別安全 |
| Vite | 7.x | 建置工具 |

### UI 元件庫
| 技術 | 版本 | 用途 |
|------|------|------|
| MUI (Material-UI) | 7.x | UI 元件庫 |
| MUI X DataGrid | 8.x | 資料表格 |
| Recharts | 2.x | 圖表視覺化 |

### 狀態與路由
| 技術 | 版本 | 用途 |
|------|------|------|
| Zustand | 5.x | 狀態管理 |
| React Router | 7.x | 路由管理 |
| i18next | 25.x | 國際化 |

### HTTP 與工具
| 技術 | 版本 | 用途 |
|------|------|------|
| Axios | 1.x | HTTP 客戶端 |
| date-fns | 4.x | 日期格式化 |

---

## 📂 專案結構

```
src/
├── api/                          # API 服務層
│   ├── client.ts                 # Axios 實例配置
│   └── services.ts               # API 方法封裝
│
├── components/                   # 可重用元件
│   ├── Breadcrumbs.tsx          # 麵包屑導航
│   ├── EmptyState.tsx           # 空狀態提示
│   ├── FileContextMenu.tsx      # 右鍵選單
│   ├── FileList.tsx             # 檔案列表 (DataGrid)
│   ├── FileListSkeleton.tsx     # 骨架屏載入
│   ├── FilePreviewModal.tsx     # 檔案預覽
│   ├── FolderTree.tsx           # 資料夾樹狀結構
│   ├── InfoDrawer.tsx           # 檔案詳情側欄
│   ├── RenameDialog.tsx         # 重新命名對話框
│   ├── SearchBar.tsx            # 搜尋欄
│   ├── ShareDialog.tsx          # 分享對話框
│   ├── UploadManager.tsx        # 上傳進度管理器
│   ├── UploadZone.tsx           # 拖曳上傳區
│   └── VersionHistoryPanel.tsx  # 版本歷程面板
│
├── layout/
│   └── MainLayout.tsx           # 主版面配置
│
├── locales/                     # 國際化翻譯
│   ├── en.json                  # 英文
│   ├── zh-CN.json               # 簡體中文
│   └── zh-TW.json               # 繁體中文
│
├── pages/                       # 頁面元件
│   ├── Dashboard.tsx            # 儀表板
│   ├── FileExplorer.tsx         # 檔案總管
│   ├── SearchResults.tsx        # 搜尋結果
│   ├── Settings.tsx             # 設定頁面
│   └── Upload.tsx               # 上傳頁面
│
├── store/
│   └── fileStore.ts             # Zustand 狀態管理
│
├── theme/
│   ├── theme.ts                 # 主題定義
│   └── ThemeContext.tsx         # 主題 Context
│
├── types/
│   └── api.ts                   # TypeScript 類型定義
│
├── App.tsx                      # 應用程式入口
├── i18n.ts                      # i18n 配置
└── main.tsx                     # React 入口點
```

---

## 🚀 快速開始

### 系統需求

- Node.js 18+
- npm 9+ 或 yarn 1.22+
- 後端 API 服務運行於 `http://127.0.0.1:8000`

### 安裝步驟

1. **下載專案**
```bash
git clone <repository-url>
cd frontend
```

2. **安裝依賴**
```bash
npm install
```

3. **啟動開發伺服器**
```bash
npm run dev
```

4. **開啟瀏覽器**
```
http://localhost:5173
```

### 建置生產版本

```bash
npm run build
```

建置輸出位於 `dist/` 目錄。

### 預覽生產版本

```bash
npm run preview
```

---

## 📖 功能使用說明

### 1. 儀表板 (Dashboard)

首頁儀表板顯示系統統計資訊：
- **檔案總數** - 系統中的檔案數量
- **已使用空間** - 總儲存空間使用量
- **分類數** - 檔案分類數量
- **分類分布圓餅圖** - 各類型檔案比例
- **儲存空間儀表** - 空間使用百分比

---

### 2. 檔案總管 (File Explorer)

#### 瀏覽檔案
- 左側顯示資料夾樹狀結構
- 中間為檔案列表 (支援排序)
- 雙擊資料夾進入，雙擊檔案預覽

#### 右鍵選單功能
| 選項 | 功能 | 適用對象 |
|------|------|----------|
| 新增資料夾 | 建立新資料夾 | 任意位置 |
| 下載 | 下載檔案 | 僅檔案 |
| 分享 | 產生分享連結 | 僅檔案 |
| 詳細資訊 | 開啟檔案資訊側欄 | 僅檔案 |
| 版本歷程 | 查看版本紀錄 | 僅檔案 |
| 重新命名 | 修改名稱 | 檔案/資料夾 |
| 刪除 | 刪除項目 | 檔案/資料夾 |

#### 版本控制
1. 右鍵點擊檔案 → 「版本歷程」
2. 在版本歷程面板中可：
   - 📥 下載歷史版本
   - 🔄 還原到特定版本
   - 🗑️ 刪除不需要的版本

#### 分享檔案
1. 右鍵點擊檔案 → 「分享」
2. 使用滑桿設定過期時間 (1小時~30天)
3. 點擊「產生連結」
4. 點擊複製按鈕取得連結

---

### 3. 上傳檔案 (Upload)

#### 方式一：拖曳上傳
- 將檔案拖曳到任何頁面即可上傳
- 畫面會顯示藍色虛線框提示

#### 方式二：上傳頁面
1. 點擊側邊欄「上傳」
2. 選擇目標資料夾
3. 點擊上傳區域或拖曳檔案
4. 查看右下角上傳進度

#### 支援的檔案類型
- **文件**: PDF, DOC, DOCX, TXT, XLS, XLSX, PPT, PPTX
- **圖片**: JPG, JPEG, PNG, GIF, BMP, SVG, WEBP
- **壓縮檔**: ZIP, RAR, 7Z, TAR, GZ

---

### 4. 標籤管理

1. 右鍵點擊檔案 → 「詳細資訊」
2. 在側欄底部的標籤區塊：
   - 輸入標籤名稱並按 Enter 或點擊「儲存」新增
   - 點擊標籤上的 X 刪除標籤

---

### 5. 搜尋

1. 在頂部搜尋欄輸入關鍵字
2. 按 Enter 執行搜尋
3. 搜尋結果頁面顯示符合的檔案

---

### 6. 設定 (Settings)

- **外觀**: 切換明暗主題
- **語言**: 選擇介面語言 (繁中/簡中/英文)
- **關於**: 查看版本與技術堆疊資訊

---

## 🔧 API 配置

後端 API 位址在 `src/api/client.ts` 中設定：

```typescript
const client = axios.create({
    baseURL: 'http://127.0.0.1:8000',
    timeout: 10000,
});
```

如需修改後端位址，請編輯此檔案。

---

## 🌍 國際化

### 新增翻譯

翻譯檔案位於 `src/locales/` 目錄：

```json
// zh-TW.json
{
    "common": {
        "save": "儲存",
        "cancel": "取消"
    }
}
```

### 使用翻譯

```tsx
import { useTranslation } from 'react-i18next';

const MyComponent = () => {
    const { t } = useTranslation();
    return <button>{t('common.save')}</button>;
};
```

---

## 📜 指令列表

| 指令 | 說明 |
|------|------|
| `npm run dev` | 啟動開發伺服器 |
| `npm run build` | 建置生產版本 |
| `npm run preview` | 預覽生產版本 |
| `npm run lint` | 執行 ESLint 檢查 |

---

## 📄 授權

MIT License

---

## 👥 貢獻

歡迎提交 Issue 和 Pull Request！
